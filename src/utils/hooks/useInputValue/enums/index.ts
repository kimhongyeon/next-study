enum eInputValueType {
    ONLY_NUMBER,
    ONLY_STRING,
    PHONE,
}

enum eInputValidationType {
    NAME,
    PHONE,
    EMAIL,
    BIRTH_DAY,
    MONTH,
    GENDER,
    LENGTH,
}

enum eInputAutoInputType {
    PHONE,
}

export { eInputValueType, eInputValidationType, eInputAutoInputType };
